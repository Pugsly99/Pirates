class Locations {
  constructor(id, description) {
    this.id = id;
    this.description = description;
  }
}

// Ship locations will be tied to then allowing a path of options for physical locations
